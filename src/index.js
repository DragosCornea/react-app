import React from 'react';
import ReactDOM from 'react-dom'; // import ReactDom object from library

import './index.css';
import App from './App';

ReactDOM.render(<App />,  document.getElementById('root'));// render met, parse html code , tell react where should be render in DOM